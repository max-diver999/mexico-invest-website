---
interface Props {
  variant?: 'header' | 'drawer';
}
const { variant = 'header' } = Astro.props;
---
<button
  type="button"
  class={`site-fx-toggle site-fx-toggle--${variant}`}
  data-site-fx-toggle
  aria-pressed="false"
  title="Show Mexican peso equivalents alongside US dollar figures"
>
  <span aria-hidden="true">MX$</span>
  <span class="site-fx-toggle__label">Show MXN</span>
</button>

<style>
  .site-fx-toggle {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    min-height: 36px;
    padding: 0.35rem 0.7rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-surface);
    color: var(--color-muted);
    font-family: inherit;
    font-size: 0.82rem;
    font-weight: 500;
    cursor: pointer;
    white-space: nowrap;
  }
  .site-fx-toggle[aria-pressed='true'] {
    background: var(--color-accent-soft);
    border-color: var(--color-accent);
    color: var(--color-accent-strong);
  }
  .site-fx-toggle--drawer {
    width: 100%;
    min-height: 44px;
    justify-content: center;
    margin-bottom: 1.25rem;
    font-size: 0.9rem;
  }
  @media (max-width: 1100px) {
    .site-fx-toggle--header { display: none; }
  }
</style>
