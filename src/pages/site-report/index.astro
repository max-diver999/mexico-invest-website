---
export const prerender = true;

const reportDate = '25 June 2026';
const reportVersion = 'v4.0';
const launchDate = '5 June 2026';
const dataThrough = '22 June 2026';

const monthlyGsc = [
  { month: 'Jun 2026', label: 'Jun', clicks: 1, impressions: 94, position: 9.1, note: 'Launch 5 Jun · through 22 Jun · first click 22 Jun' },
];

const monthlyGa4 = [
  { month: 'Jun', sessions: 78 },
];

const contentBreakdown = [
  { type: 'Guides', count: 116, words: 329468, color: '#b45309' },
  { type: 'Projects', count: 100, words: 157934, color: '#4e9e7e' },
  { type: 'Comparisons', count: 34, words: 80034, color: '#d97706' },
  { type: 'Areas', count: 32, words: 79867, color: '#8b5cf6' },
  { type: 'Developers', count: 9, words: 18554, color: '#60a5fa' },
  { type: 'News', count: 7, words: 5553, color: '#78716c' },
];

const totalClicks = monthlyGsc.reduce((s, m) => s + m.clicks, 0);
const totalImp = monthlyGsc.reduce((s, m) => s + m.impressions, 0);
const totalWords = contentBreakdown.reduce((s, c) => s + c.words, 0);
const totalFiles = contentBreakdown.reduce((s, c) => s + c.count, 0);
const clicksDelta = 0;
const maxImp = Math.max(...monthlyGsc.map(m => m.impressions));
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex,nofollow" />
  <title>mexico-invest.com — Site Report</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif; background: #f4f1eb; color: #2c2c2c; min-height: 100vh; }

    .header { background: #1a2e2a; color: white; padding: 32px 40px; display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 16px; }
    .header-brand { display: flex; align-items: center; gap: 12px; }
    .header-logo { width: 36px; height: 36px; background: #4e9e7e; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-weight: 800; font-size: 18px; color: white; }
    .header h1 { font-size: 22px; font-weight: 700; }
    .header-sub { font-size: 13px; color: #9dbfb3; margin-top: 2px; }
    .header-meta { text-align: right; }
    .header-meta .label { font-size: 12px; color: #9dbfb3; }
    .header-meta .value { font-size: 14px; font-weight: 600; margin-top: 2px; }
    .live-badge { display: inline-flex; align-items: center; gap: 6px; background: #2d5a4a; border-radius: 20px; padding: 4px 12px; font-size: 12px; margin-top: 8px; }
    .live-dot { width: 6px; height: 6px; background: #4ade80; border-radius: 50%; animation: pulse 2s infinite; }
    @keyframes pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.4; } }

    .container { max-width: 1280px; margin: 0 auto; padding: 32px 24px; }

    .section-title { font-size: 13px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.08em; color: #7a7a6e; margin-bottom: 16px; margin-top: 40px; }
    .section-title:first-child { margin-top: 0; }

    .stats-grid { display: grid; grid-template-columns: repeat(7, minmax(0, 1fr)); gap: 10px; }
    .stat-card { background: white; border-radius: 10px; padding: 14px 10px; border: 1px solid #e8e4dc; min-width: 0; }
    .stat-card .num { font-size: 26px; font-weight: 800; color: #1a2e2a; line-height: 1.05; letter-spacing: -0.02em; }
    .stat-card .num.teal { color: #2d7a5e; }
    .stat-card .num.amber { color: #b45309; }
    .stat-card .label { font-size: 11px; color: #7a7a6e; margin-top: 5px; line-height: 1.25; }
    .stat-card .sublabel { font-size: 9px; color: #aaa; margin-top: 3px; line-height: 1.35; }
    @media (max-width: 1100px) { .stats-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); } }

    .info-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    @media (max-width: 600px) { .info-grid { grid-template-columns: 1fr; } }
    .info-card { background: white; border-radius: 12px; padding: 20px; border: 1px solid #e8e4dc; }
    .info-card h3 { font-size: 14px; font-weight: 700; color: #1a2e2a; margin-bottom: 14px; padding-bottom: 10px; border-bottom: 1px solid #f0ece4; }
    .info-row { display: flex; justify-content: space-between; align-items: flex-start; padding: 6px 0; border-bottom: 1px solid #f7f4ef; gap: 16px; }
    .info-row:last-child { border-bottom: none; }
    .info-row .key { font-size: 13px; color: #7a7a6e; flex-shrink: 0; }
    .info-row .val { font-size: 13px; font-weight: 600; color: #2c2c2c; text-align: right; }
    .info-row .val a { color: #2d7a5e; text-decoration: none; }
    .info-row .val a:hover { text-decoration: underline; }

    .content-card { background: white; border-radius: 12px; border: 1px solid #e8e4dc; overflow: hidden; }
    .table-scroll { width: 100%; overflow-x: auto; -webkit-overflow-scrolling: touch; }
    .content-table { width: 100%; border-collapse: collapse; }
    .content-table th { font-size: 11px; text-transform: uppercase; letter-spacing: 0.06em; color: #7a7a6e; font-weight: 700; padding: 12px 20px; text-align: left; background: #faf8f4; border-bottom: 1px solid #e8e4dc; }
    .content-table td { padding: 12px 20px; font-size: 13px; border-bottom: 1px solid #f4f1eb; vertical-align: top; }
    .content-table tr:last-child td { border-bottom: none; }
    .content-table tr:hover td { background: #faf8f4; }
    .content-table .count { font-weight: 800; color: #2d7a5e; font-size: 18px; }
    .content-table .words { color: #7a7a6e; font-size: 12px; }
    .tag { display: inline-block; padding: 2px 8px; border-radius: 20px; font-size: 11px; font-weight: 600; }
    .tag.green { background: #dcfce7; color: #166534; }
    .tag.blue { background: #dbeafe; color: #1e40af; }
    .tag.amber { background: #fef3c7; color: #92400e; }
    .tag.gray { background: #f3f4f6; color: #374151; }

    .changelog { display: flex; flex-direction: column; gap: 0; }
    .changelog-item { background: white; border-radius: 0; border: 1px solid #e8e4dc; border-bottom: none; padding: 16px 20px; display: flex; gap: 16px; align-items: flex-start; }
    .changelog-item:first-child { border-radius: 12px 12px 0 0; }
    .changelog-item:last-child { border-radius: 0 0 12px 12px; border-bottom: 1px solid #e8e4dc; }
    .changelog-date { font-size: 11px; color: #9ca3af; min-width: 80px; padding-top: 2px; }
    .changelog-content { flex: 1; }
    .changelog-title { font-size: 13px; font-weight: 600; color: #1a2e2a; }
    .changelog-desc { font-size: 12px; color: #7a7a6e; margin-top: 3px; }
    .changelog-tags { display: flex; gap: 6px; flex-wrap: wrap; margin-top: 6px; }

    .next-steps { display: flex; flex-direction: column; gap: 10px; }
    .next-item { background: white; border-radius: 10px; border: 1px solid #e8e4dc; padding: 14px 18px; display: flex; align-items: flex-start; gap: 12px; }
    .next-item .priority { font-size: 11px; font-weight: 700; min-width: 50px; padding-top: 1px; }
    .priority.high { color: #dc2626; }
    .priority.medium { color: #d97706; }
    .priority.low { color: #059669; }
    .next-item .text { font-size: 13px; color: #2c2c2c; }
    .next-item .subtext { font-size: 12px; color: #9ca3af; margin-top: 2px; }

    .links-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 12px; }
    .link-card { background: white; border-radius: 10px; border: 1px solid #e8e4dc; padding: 16px; text-decoration: none; color: #2c2c2c; transition: border-color 0.2s; display: block; }
    .link-card:hover { border-color: #2d7a5e; }
    .link-card .link-label { font-size: 11px; color: #9ca3af; text-transform: uppercase; letter-spacing: 0.06em; }
    .link-card .link-url { font-size: 13px; font-weight: 600; color: #2d7a5e; margin-top: 4px; word-break: break-all; }

    .footer { text-align: center; padding: 32px 24px; color: #9ca3af; font-size: 12px; }
    .footer strong { color: #7a7a6e; }

    /* ── SEO PULSE DASHBOARD ── */
    .pulse-header { display: flex; align-items: center; justify-content: space-between; flex-wrap: wrap; gap: 12px; margin-bottom: 20px; }
    .pulse-header h2 { font-size: 16px; font-weight: 800; color: #1a2e2a; }
    .pulse-updated { font-size: 11px; color: #9ca3af; background: #f4f1eb; border-radius: 20px; padding: 4px 12px; display: flex; align-items: center; gap: 6px; }
    .pulse-updated-dot { width: 6px; height: 6px; border-radius: 50%; background: #4ade80; animation: pulse 2s infinite; }

    .pulse-kpi-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 14px; margin-bottom: 20px; }
    @media (max-width: 700px) { .pulse-kpi-row { grid-template-columns: repeat(2, 1fr); } }
    .pulse-kpi { background: white; border-radius: 12px; border: 1px solid #e8e4dc; padding: 18px 20px; position: relative; overflow: hidden; }
    .pulse-kpi::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; }
    .pulse-kpi.kpi-green::before { background: linear-gradient(90deg, #4ade80, #2d7a5e); }
    .pulse-kpi.kpi-blue::before { background: linear-gradient(90deg, #60a5fa, #2563eb); }
    .pulse-kpi.kpi-amber::before { background: linear-gradient(90deg, #fbbf24, #d97706); }
    .pulse-kpi.kpi-orange::before { background: linear-gradient(90deg, #fb923c, #c2410c); }
    .pulse-kpi .kpi-val { font-size: 36px; font-weight: 900; color: #1a2e2a; line-height: 1; margin-top: 4px; }
    .pulse-kpi .kpi-label { font-size: 11px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.07em; color: #7a7a6e; }
    .pulse-kpi .kpi-sub { font-size: 11px; color: #aaa; margin-top: 4px; }
    .pulse-kpi .kpi-trend { position: absolute; top: 18px; right: 18px; font-size: 11px; font-weight: 700; padding: 2px 8px; border-radius: 20px; }
    .trend-up { background: #dcfce7; color: #166534; }
    .trend-new { background: #dbeafe; color: #1e40af; }

    .pulse-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
    @media (max-width: 700px) { .pulse-grid { grid-template-columns: 1fr; } }

    .pulse-card { background: white; border-radius: 12px; border: 1px solid #e8e4dc; padding: 18px 20px; }
    .pulse-card-title { font-size: 12px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.07em; color: #7a7a6e; margin-bottom: 14px; display: flex; align-items: center; gap: 8px; }
    .pulse-card-title span { width: 8px; height: 8px; border-radius: 50%; display: inline-block; }
    .span-green { background: #4ade80; }
    .span-blue { background: #60a5fa; }

    .pulse-query-row { display: flex; align-items: center; gap: 10px; padding: 7px 0; border-bottom: 1px solid #f4f1eb; }
    .pulse-query-row:last-child { border-bottom: none; }
    .pulse-query-rank { font-size: 11px; font-weight: 700; color: #aaa; min-width: 20px; }
    .pulse-query-text { flex: 1; min-width: 0; font-size: 12px; color: #2c2c2c; white-space: normal; overflow-wrap: anywhere; word-break: break-word; }
    .pulse-query-pos { font-size: 11px; font-weight: 700; color: #2d7a5e; background: #f0faf5; border-radius: 6px; padding: 2px 7px; }
    .pulse-query-imp { font-size: 11px; color: #aaa; min-width: 50px; text-align: right; }

    .pulse-page-row { display: flex; align-items: flex-start; gap: 10px; padding: 7px 0; border-bottom: 1px solid #f4f1eb; }
    .pulse-page-row:last-child { border-bottom: none; }
    .pulse-page-url { flex: 1; min-width: 0; font-size: 11px; color: #2d7a5e; overflow-wrap: anywhere; word-break: break-word; }
    .pulse-page-meta { display: flex; gap: 6px; flex-shrink: 0; }
    .pulse-badge { font-size: 10px; font-weight: 700; padding: 2px 7px; border-radius: 20px; }
    .badge-clicks { background: #dcfce7; color: #166534; }
    .badge-impr { background: #dbeafe; color: #1e40af; }
    .badge-pos { background: #fef3c7; color: #92400e; }

    .pulse-chart { margin-top: 4px; }
    .chart-bars { display: flex; align-items: flex-end; gap: 4px; height: 60px; }
    .chart-bar-wrap { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 4px; }
    .chart-bar { width: 100%; border-radius: 3px 3px 0 0; min-height: 2px; transition: height 0.3s; }
    .chart-bar.bar-impr { background: #bfdbfe; }
    .chart-bar.bar-click { background: #4ade80; }
    .chart-label { font-size: 9px; color: #aaa; }

    .pulse-insight { background: #f0faf5; border: 1px solid #c6e8da; border-radius: 10px; padding: 12px 16px; margin-top: 16px; display: flex; gap: 12px; align-items: flex-start; }
    .pulse-insight-icon { font-size: 18px; flex-shrink: 0; }
    .pulse-insight-text { font-size: 12px; color: #1a4f3a; line-height: 1.5; }
    .pulse-insight-text strong { font-weight: 700; }

    .pulse-footer-note { font-size: 11px; color: #aaa; margin-top: 16px; text-align: center; }
    .pulse-footer-note a { color: #2d7a5e; text-decoration: none; }
    .pulse-table-scroll { width: 100%; overflow-x: auto; -webkit-overflow-scrolling: touch; }
    .pulse-table-scroll table { min-width: 720px; }

    @media (max-width: 640px) {
      body { overflow-x: hidden; }
      .header { padding: 22px 16px; display: block; }
      .header-brand { align-items: flex-start; }
      .header h1 { font-size: 19px; line-height: 1.2; word-break: break-word; }
      .header-meta { text-align: left; margin-top: 18px; }
      .live-badge { display: flex; align-items: flex-start; width: 100%; border-radius: 14px; padding: 8px 10px; line-height: 1.45; white-space: normal; }
      .container { width: 100%; padding: 20px 12px; overflow: hidden; }
      .section-title { margin-top: 30px !important; font-size: 12px; line-height: 1.35; }
      .stats-grid, .info-grid, .pulse-grid, .pulse-kpi-row, .links-grid { grid-template-columns: 1fr !important; gap: 10px; }
      .pulse-card > div[style*="grid-template-columns:repeat(4,1fr)"],
      .pulse-card > div[style*="grid-template-columns:1fr 1fr"],
      div[style*="grid-template-columns:1fr 1fr"] { grid-template-columns: 1fr !important; }
      .stat-card, .info-card, .pulse-card, .pulse-kpi, .link-card { padding: 14px; border-radius: 12px; max-width: 100%; }
      .stat-card .num, .pulse-kpi .kpi-val { font-size: 30px; }
      .pulse-kpi .kpi-trend { position: static; display: inline-block; margin-bottom: 8px; }
      .info-row { display: block; }
      .info-row .key, .info-row .val { display: block; text-align: left; }
      .info-row .val { margin-top: 4px; word-break: break-word; }
      .pulse-query-row, .pulse-page-row { align-items: flex-start; flex-wrap: wrap; gap: 6px; }
      .pulse-query-text, .pulse-page-url { min-width: 0; white-space: normal; overflow: visible; text-overflow: clip; word-break: break-word; }
      .pulse-page-meta { flex-wrap: wrap; width: 100%; }
      .pulse-query-imp { min-width: auto; text-align: left; }
      .pulse-insight { padding: 12px; gap: 10px; }
      .pulse-insight-text { font-size: 12px; word-break: break-word; }
      .pulse-table-scroll { margin: 0 -2px; }
      .content-card { overflow-x: auto; -webkit-overflow-scrolling: touch; }
      .content-table { min-width: 720px; }
      .changelog-item { display: block; padding: 14px; }
      .changelog-date { min-width: 0; margin-bottom: 6px; }
      .footer { padding: 24px 14px; line-height: 1.5; }
    }

    /* ── GROWTH DASHBOARD v19 ── */
    :root {
      --ink: #1a2e2a;
      --ink-soft: #2c4a44;
      --accent: #4e9e7e;
      --accent-dark: #2d7a5e;
      --paper-2: #faf8f4;
      --card: #ffffff;
      --border: #e8e4dc;
      --muted: #7a7a6e;
      --up: #16a34a;
      --down: #dc2626;
      --shadow: 0 4px 24px rgba(26, 46, 42, 0.06);
      --radius: 14px;
    }

    .kpi-strip {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 12px;
      margin-top: 8px;
      margin-bottom: 32px;
    }
    @media (max-width: 1100px) { .kpi-strip { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 560px) { .kpi-strip { grid-template-columns: 1fr; } }
    .kpi-card {
      background: var(--card);
      border-radius: var(--radius);
      border: 1px solid var(--border);
      padding: 18px 16px;
      box-shadow: var(--shadow);
    }
    .kpi-label {
      font-size: 10px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--muted);
    }
    .kpi-value {
      font-size: 28px;
      font-weight: 800;
      color: var(--ink);
      margin: 6px 0 4px;
      letter-spacing: -0.02em;
      line-height: 1;
    }
    .kpi-delta {
      font-size: 11px;
      font-weight: 700;
      display: inline-flex;
      align-items: center;
      gap: 3px;
      padding: 2px 8px;
      border-radius: 999px;
    }
    .kpi-delta.up { background: #dcfce7; color: var(--up); }
    .kpi-delta.down { background: #fee2e2; color: var(--down); }
    .kpi-delta.neutral { background: #f3f4f6; color: var(--muted); }
    .kpi-sub { font-size: 10px; color: #aaa; margin-top: 6px; line-height: 1.35; }

    /* ── Sections ── */
    .section { margin-bottom: 48px; }
    .section-head {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 12px;
      margin-bottom: 20px;
    }
    .section-head h2 {
      font-family: Georgia, 'Times New Roman', serif;
      font-size: 24px;
      font-weight: 700;
      color: var(--ink);
    }
    .section-head p { font-size: 13px; color: var(--muted); max-width: 480px; }
    .badge-src {
      font-size: 11px;
      font-weight: 600;
      color: var(--accent-dark);
      background: #f0faf5;
      border: 1px solid #c6e8da;
      border-radius: 999px;
      padding: 4px 12px;
    }

    .grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
    .grid-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 16px; }
    @media (max-width: 900px) { .grid-2, .grid-3 { grid-template-columns: 1fr; } }

    .card {
      background: var(--card);
      border-radius: var(--radius);
      border: 1px solid var(--border);
      padding: 24px;
      box-shadow: var(--shadow);
    }
    .card-title {
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--muted);
      margin-bottom: 16px;
    }
    .chart-wrap { position: relative; height: 280px; }
    .chart-wrap-sm { height: 220px; }

    /* ── Monthly table ── */
    .data-table { width: 100%; border-collapse: collapse; font-size: 13px; }
    .data-table th {
      text-align: left;
      font-size: 10px;
      text-transform: uppercase;
      letter-spacing: 0.07em;
      color: var(--muted);
      padding: 10px 14px;
      border-bottom: 2px solid var(--border);
      font-weight: 700;
    }
    .data-table td {
      padding: 12px 14px;
      border-bottom: 1px solid #f4f1eb;
      vertical-align: middle;
    }
    .data-table tr:last-child td { border-bottom: none; }
    .data-table tr:hover td { background: var(--paper-2); }
    .data-table .num { font-weight: 800; font-size: 15px; color: var(--accent-dark); }
    .bar-cell { display: flex; align-items: center; gap: 10px; }
    .mini-bar {
      height: 6px;
      border-radius: 3px;
      background: linear-gradient(90deg, var(--accent), var(--accent-dark));
      min-width: 4px;
    }

    /* ── Query / page rows ── */
    .rank-row {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 9px 0;
      border-bottom: 1px solid #f4f1eb;
      font-size: 12px;
    }
    .rank-row:last-child { border-bottom: none; }
    .rank-num { font-weight: 800; color: #ccc; min-width: 22px; font-size: 11px; }
    .rank-text { flex: 1; min-width: 0; word-break: break-word; color: var(--ink); }
    .rank-url { flex: 1; min-width: 0; word-break: break-all; color: var(--accent-dark); font-size: 11px; }
    .pill {
      font-size: 10px;
      font-weight: 700;
      padding: 2px 8px;
      border-radius: 999px;
      white-space: nowrap;
    }
    .pill-green { background: #dcfce7; color: #166534; }
    .pill-blue { background: #dbeafe; color: #1e40af; }
    .pill-amber { background: #fef3c7; color: #92400e; }

    /* ── Insight ── */
    .insight {
      background: linear-gradient(135deg, #f0faf5, #ecfdf5);
      border: 1px solid #c6e8da;
      border-radius: 12px;
      padding: 16px 20px;
      font-size: 13px;
      color: var(--ink-soft);
      line-height: 1.55;
      margin-top: 20px;
    }
    .insight strong { color: var(--ink); }

    /* ── Timeline ── */
    .timeline { position: relative; padding-left: 28px; }
    .timeline::before {
      content: '';
      position: absolute;
      left: 7px;
      top: 8px;
      bottom: 8px;
      width: 2px;
      background: linear-gradient(180deg, var(--accent), var(--border));
    }
    .tl-item { position: relative; padding-bottom: 24px; }
    .tl-item:last-child { padding-bottom: 0; }
    .tl-dot {
      position: absolute;
      left: -28px;
      top: 4px;
      width: 14px;
      height: 14px;
      border-radius: 50%;
      background: var(--accent);
      border: 3px solid var(--paper);
      box-shadow: 0 0 0 2px var(--accent);
    }
    .tl-date { font-size: 11px; color: var(--muted); font-weight: 600; }
    .tl-title { font-size: 14px; font-weight: 700; color: var(--ink); margin: 2px 0 4px; }
    .tl-desc { font-size: 12px; color: var(--muted); line-height: 1.45; }
    .tl-tag {
      display: inline-block;
      font-size: 10px;
      font-weight: 700;
      padding: 2px 8px;
      border-radius: 999px;
      background: #f0faf5;
      color: var(--accent-dark);
      margin-top: 6px;
    }


  </style>
  <script is:inline src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
</head>
<body>

<div class="header">
  <div class="header-brand">
    <div class="header-logo">MX</div>
    <div>
      <div class="header-sub">Website Performance Report — Mexico RE</div>
      <h1>mexico-invest.com</h1>
    </div>
  </div>
  <div class="header-meta">
    <div class="label">Last updated</div>
    <div class="value">{reportDate} · {reportVersion} (live)</div>
    <div class="live-badge"><span class="live-dot"></span> 298 articles · 309 sitemap · indexing complete</div>
    <div style="margin-top:10px;font-size:12px;"><a href="https://mexico-invest.com/portfolio-report/" target="_blank" style="color:#9dbfb3;text-decoration:none;">← Portfolio hub (all sites)</a></div>
  </div>
</div>

<div class="container">

  <div class="section-title">At a glance</div>
  <div class="stats-grid">
    <div class="stat-card">
      <div class="num teal">309</div>
      <div class="label">URLs in sitemap</div>
      <div class="sublabel">100 projects · 116 guides · 32 areas · 34 comparisons · 7 news</div>
    </div>
    <div class="stat-card">
      <div class="num amber">New</div>
      <div class="label">Domain authority</div>
      <div class="sublabel">Fresh domain · launched 5 Jun · baseline TBD</div>
    </div>
    <div class="stat-card">
      <div class="num">~671K</div>
      <div class="label">SEO words</div>
      <div class="sublabel">298 MDX files · ~2 253 avg · 57 git commits</div>
    </div>
    <div class="stat-card">
      <div class="num teal">1</div>
      <div class="label">GSC click URLs</div>
      <div class="sublabel">1 Jun–22 Jun · first click 22 Jun (cfdi guide)</div>
    </div>
    <div class="stat-card">
      <div class="num">6</div>
      <div class="label">Content collections</div>
      <div class="sublabel">guides · projects · areas · compare · developers · news</div>
    </div>
    <div class="stat-card">
      <div class="num amber">1</div>
      <div class="label">GSC clicks</div>
      <div class="sublabel">94 imp · CTR 1.06% · avg pos 9.1</div>
    </div>
    <div class="stat-card">
      <div class="num teal">78</div>
      <div class="label">GA4 sessions</div>
      <div class="sublabel">1 organic · Bing 3 imp · lead API 200</div>
    </div>
  </div>


  <!-- ═══ GROWTH DASHBOARD v19 — charts + MoM (CTR hidden) ═══ -->
  <div class="section-title" style="margin-top:32px;">Growth dashboard — since launch</div>

  <div class="kpi-strip">
    <div class="kpi-card">
      <div class="kpi-label">GSC Clicks</div>
      <div class="kpi-value">{totalClicks}</div>
      <span class="kpi-delta up">↑ First click 22 Jun</span>
      <div class="kpi-sub">Since launch · Google Search Console</div>
    </div>
    <div class="kpi-card">
      <div class="kpi-label">Impressions</div>
      <div class="kpi-value">94</div>
      <span class="kpi-delta up">↑ Index warming</span>
      <div class="kpi-sub">94 total · peak 17/day 21–22 Jun</div>
    </div>
    <div class="kpi-card">
      <div class="kpi-label">GA4 Sessions</div>
      <div class="kpi-value">78</div>
      <span class="kpi-delta neutral">Jun only</span>
      <div class="kpi-sub">Since launch (Jun 6–22)</div>
    </div>
    <div class="kpi-card">
      <div class="kpi-label">Content corpus</div>
      <div class="kpi-value">0.7M</div>
      <span class="kpi-delta neutral">{totalFiles} pages</span>
      <div class="kpi-sub">Words in MDX</div>
    </div>
  </div>

  <section class="section">
    <div class="section-head">
      <div>
        <h2>Monthly growth since launch</h2>
        <p>Google Search Console web search · {launchDate} → {dataThrough}. June is partial (20 days).</p>
      </div>
      <span class="badge-src">GSC API · Updated {reportDate}</span>
    </div>
    <div class="grid-2">
      <div class="card">
        <div class="card-title">Clicks &amp; impressions by month</div>
        <div class="chart-wrap"><canvas id="chartMonthly"></canvas></div>
      </div>
      <div class="card">
        <div class="card-title">GA4 sessions by month</div>
        <div class="chart-wrap"><canvas id="chartGa4"></canvas></div>
        <div class="insight" style="margin-top:16px;">
          <strong>Launch traffic:</strong> 78 sessions in 17 days (mostly direct/pre-launch QA). 1 organic session so far — expected to grow as 309 URLs finish indexing.
        </div>
      </div>
    </div>

    <div class="card" style="margin-top:20px;">
      <div class="card-title">Month-over-month detail</div>
      <div style="overflow-x:auto;">
        <table class="data-table">
          <thead>
            <tr>
              <th>Month</th>
              <th>Clicks</th>
              <th>Impressions</th>
              <th>Avg position</th>
              <th>MoM clicks</th>
              <th>Notes</th>
            </tr>
          </thead>
          <tbody>
        <tr>
          <td><strong>Guides</strong><br><span style="font-size:11px;color:#9ca3af">Foreign ownership, fideicomiso, AMPI, tax/CFDI, STR, insurance, buyer nationality, investment strategy</span></td>
          <td><span class="count">116</span></td>
          <td><span class="words">~2 841 words</span></td>
          <td><span class="tag blue">Head keywords</span></td>
          <td><span class="tag green">Live</span></td>
        </tr>
        <tr>
          <td><strong>Area guides — 32 Mexico markets</strong><br><span style="font-size:11px;color:#9ca3af">Playa del Carmen, Tulum, Cancún, Los Cabos, Puerto Vallarta, Mérida, CDMX zones and more</span></td>
          <td><span class="count">32</span></td>
          <td><span class="words">~2 496 words</span></td>
          <td><span class="tag blue">Location intent</span></td>
          <td><span class="tag green">Live</span></td>
        </tr>
        <tr>
          <td><strong>Project reviews</strong><br><span style="font-size:11px;color:#9ca3af">100 developments · Cloudinary photos ×3 each</span></td>
          <td><span class="count">100</span></td>
          <td><span class="words">~1 579 words</span></td>
          <td><span class="tag blue">Project intent</span></td>
          <td><span class="tag green">Live</span></td>
        </tr>
        <tr>
          <td><strong>Comparisons</strong><br><span style="font-size:11px;color:#9ca3af">Mexico vs USA/Spain/Portugal, Playa vs Tulum, fideicomiso vs direct</span></td>
          <td><span class="count">34</span></td>
          <td><span class="words">~2 354 words</span></td>
          <td><span class="tag amber">Comparison intent</span></td>
          <td><span class="tag green">Live</span></td>
        </tr>
        <tr>
          <td><strong>Developer profiles</strong><br><span style="font-size:11px;color:#9ca3af">9 major developers — AMPI-verified brokers, track record</span></td>
          <td><span class="count">9</span></td>
          <td><span class="words">~2 061 words</span></td>
          <td><span class="tag blue">Developer intent</span></td>
          <td><span class="tag green">Live</span></td>
        </tr>
        <tr>
          <td><strong>News</strong><br><span style="font-size:11px;color:#9ca3af">Banxico FX, Playa resale, Los Cabos luxury · target 3/week</span></td>
          <td><span class="count">7</span></td>
          <td><span class="words">~793 words</span></td>
          <td><span class="tag gray">NewsArticle Schema</span></td>
          <td><span class="tag amber">Scale up</span></td>
        </tr>
        <tr style="background:#fffbeb;">
          <td><strong>TOTAL</strong></td>
          <td><span class="count" style="font-size:24px;">298</span></td>
          <td><span class="words">~2 253 avg</span></td>
          <td></td>
          <td><span class="tag green">qa:corpus PASS</span></td>
        </tr>
      </tbody>
    </table>
  </div>

  <div class="section-title">Change history</div>
  <div class="changelog">
      <div class="changelog-item">
        <div class="changelog-date">25 Jun 2026</div>
        <div class="changelog-content">
          <div class="changelog-title">Site report v4.0 — full MORE Group dashboard format + GSC/GA4/Bing refresh</div>
          <div class="changelog-desc">Growth dashboard with Chart.js: monthly GSC/GA4, daily momentum, content donut, MoM table. GSC 1 Jun–22 Jun: 1 click, 94 impressions, 15 pages in SERP. First click 22 Jun on cfdi-cost-basis-mexico (33% CTR). GA4: 78 sessions, 1 organic. Bing: 3 impressions. Indexing: 309 URLs submitted, backlog 0.</div>
          <div class="changelog-tags"><span class="tag green">v4.0</span><span class="tag blue">GSC MCP</span><span class="tag amber">Charts</span></div>
        </div>
      </div>
      <div class="changelog-item">
        <div class="changelog-date">16 Jun 2026</div>
        <div class="changelog-content">
          <div class="changelog-title">Phase B+C fix pack + 3 news + CTR sprint + indexing batch</div>
          <div class="changelog-desc">41 articles: Cloudinary images (111 photos), Quick answer stubs, em-dash humanize, strict gate. News: Banxico FX, Playa resale, Los Cabos luxury. CTR rewrite: can-foreigners-buy + airbnb guide titles. Google Indexing API 309 URLs. Build PASS.</div>
          <div class="changelog-tags"><span class="tag green">Phase B+C</span><span class="tag blue">Indexing</span><span class="tag amber">CTR</span></div>
        </div>
      </div>
      <div class="changelog-item">
        <div class="changelog-date">5–10 Jun 2026</div>
        <div class="changelog-content">
          <div class="changelog-title">Site launched — 298 MDX articles, mexico-invest-indexing GCP project</div>
          <div class="changelog-desc">mexico-invest.com live on Vercel. 116 guides, 100 projects, 32 areas, 34 comparisons, 9 developers. Dedicated GCP project for Google Indexing API. SA ownership via Site Verification API.</div>
          <div class="changelog-tags"><span class="tag green">Launch</span><span class="tag blue">298 pages</span><span class="tag amber">GCP setup</span></div>
        </div>
      </div>
  </div>

      <div class="section-title">Next steps — 25 Jun 2026</div>
  <div class="next-steps">
    <div class="next-item" style="background:#fef2f2;border-color:#fecaca;">
      <div class="priority high">P0</div>
      <div>
        <div class="text" style="font-weight:700;">CTR sprint — can-foreigners-buy (8 imp, pos 4.2, 0 clk)</div>
        <div class="subtext">Title + description live since 16 Jun · monitor GSC weekly.</div>
      </div>
    </div>
    <div class="next-item" style="background:#fff7ed;border-color:#fed7aa;">
      <div class="priority medium">P1</div>
      <div>
        <div class="text" style="font-weight:700;">News cadence — scale from 7 to 3/week</div>
        <div class="subtext">Banxico, Playa resale, Los Cabos batch live · add 2 more this week.</div>
      </div>
    </div>
    <div class="next-item" style="background:#fff7ed;border-color:#fed7aa;">
      <div class="priority medium">P1</div>
      <div>
        <div class="text" style="font-weight:700;">GA4 lead event verification</div>
        <div class="subtext">0 key events reported · verify LeadForm → GA4 conversion tracking.</div>
      </div>
    </div>
    <div class="next-item">
      <div class="priority medium">P1</div>
      <div>
        <div class="text" style="font-weight:700;">Monitor GSC click growth — re-report 2 Jul</div>
        <div class="subtext">94 imp baseline · expect 10× in 4 weeks if index holds pace.</div>
      </div>
    </div>
    <div class="next-item" style="background:#f0fdf4;border-color:#bbf7d0;">
      <div class="priority low">DONE</div>
      <div>
        <div class="text" style="font-weight:700;">Site-report v4.0 + full indexing (309 URLs)</div>
        <div class="subtext">Growth dashboard · GSC/GA4/Bing MCP refresh · Phase B+C fix pack deployed.</div>
      </div>
    </div>
  </div>

  <div class="section-title">Quick links</div>
  <div class="links-grid">
    <a class="link-card" href="https://mexico-invest.com" target="_blank">
      <div class="link-label">Live site</div>
      <div class="link-url">mexico-invest.com</div>
    </a>
    <a class="link-card" href="https://github.com/max-diver999/mexico-invest-website" target="_blank">
      <div class="link-label">GitHub repository</div>
      <div class="link-url">github.com/max-diver999/mexico-invest-website</div>
    </a>
    <a class="link-card" href="https://vercel.com/dashboard" target="_blank">
      <div class="link-label">Hosting & deploys</div>
      <div class="link-url">vercel.com/dashboard</div>
    </a>
    <a class="link-card" href="https://search.google.com/search-console" target="_blank">
      <div class="link-label">Google Search Console</div>
      <div class="link-url">search.google.com/search-console</div>
    </a>
    <a class="link-card" href="https://analytics.google.com" target="_blank">
      <div class="link-label">Google Analytics 4</div>
      <div class="link-url">analytics.google.com</div>
    </a>
    <a class="link-card" href="https://www.bing.com/webmasters" target="_blank">
      <div class="link-label">Bing Webmaster Tools</div>
      <div class="link-url">bing.com/webmasters</div>
    </a>
    <a class="link-card" href="https://mexico-invest.com/sitemap-index.xml" target="_blank">
      <div class="link-label">Sitemap</div>
      <div class="link-url">mexico-invest.com/sitemap-index.xml</div>
    </a>
  </div>

</div>

  <div class="footer">
  <strong>Mexico Invest</strong> · mexico-invest.com · Report updated 25 Jun 2026 v4.0 · 309 sitemap URLs · ~671K words · GSC 1 click / 94 imp (1 Jun–22 Jun) · GA4 78 sessions · 57 git commits<br>
  <a href="https://mexico-invest.com/portfolio-report/" style="color:#2d7a5e;text-decoration:none;">Portfolio hub (all sites)</a> ·
  To update: edit <code>src/pages/site-report/index.astro</code> in mexico-invest-website
</div>


<script type="application/json" id="chart-data">{"monthlyGsc":[{"label":"Jun","clicks":1,"impressions":94}],"monthlyGa4":[{"month":"Jun","sessions":78}],"contentBreakdown":[{"type":"Guides","count":116,"color":"#b45309"},{"type":"Projects","count":100,"color":"#4e9e7e"},{"type":"Comparisons","count":34,"color":"#d97706"},{"type":"Areas","count":32,"color":"#8b5cf6"},{"type":"Developers","count":9,"color":"#60a5fa"},{"type":"News","count":7,"color":"#78716c"}],"dailyRaw":[{"d":"06-07","c":0,"i":1},{"d":"06-12","c":0,"i":5},{"d":"06-14","c":0,"i":5},{"d":"06-17","c":0,"i":11},{"d":"06-18","c":0,"i":16},{"d":"06-21","c":0,"i":17},{"d":"06-22","c":1,"i":17}]}</script>
<script is:inline>
(function initReportCharts() {
  function run() {
    if (typeof Chart === 'undefined') { setTimeout(run, 50); return; }
    var el = document.getElementById('chart-data');
    if (!el) return;
    var data = JSON.parse(el.textContent);
    Chart.defaults.font = { family: "-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif", size: 11 };
    Chart.defaults.color = '#7a7a6e';

    var monthly = data.monthlyGsc;
    var ga4 = data.monthlyGa4;
    var content = data.contentBreakdown;
    var daily = data.dailyRaw;

    var cm = document.getElementById('chartMonthly');
    if (cm) new Chart(cm, {
      type: 'bar',
      data: {
        labels: monthly.map(function(m) { return m.label; }),
        datasets: [
          { label: 'Impressions', data: monthly.map(function(m) { return m.impressions; }), backgroundColor: 'rgba(96,165,250,0.35)', borderColor: '#60a5fa', borderWidth: 1, borderRadius: 6, yAxisID: 'y' },
          { label: 'Clicks', data: monthly.map(function(m) { return m.clicks; }), type: 'line', borderColor: '#2d7a5e', backgroundColor: 'rgba(45,122,94,0.12)', borderWidth: 3, pointRadius: 6, pointBackgroundColor: '#2d7a5e', fill: true, tension: 0.3, yAxisID: 'y1' }
        ]
      },
      options: { responsive: true, maintainAspectRatio: false, interaction: { mode: 'index', intersect: false }, plugins: { legend: { position: 'bottom' } }, scales: { y: { position: 'left', grid: { color: '#f4f1eb' }, ticks: { callback: function(v) { return v >= 1000 ? (v/1000)+'k' : v; } } }, y1: { position: 'right', grid: { display: false }, min: 0 } } }
    });

    var cg = document.getElementById('chartGa4');
    if (cg) new Chart(cg, {
      type: 'bar',
      data: { labels: ga4.map(function(m) { return m.month; }), datasets: [{ label: 'Sessions', data: ga4.map(function(m) { return m.sessions; }), backgroundColor: ['rgba(78,158,126,0.45)','rgba(78,158,126,0.55)','rgba(78,158,126,0.7)','rgba(78,158,126,0.85)'], borderColor: '#2d7a5e', borderWidth: 1, borderRadius: 8 }] },
      options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { display: false } }, scales: { y: { grid: { color: '#f4f1eb' }, beginAtZero: true }, x: { grid: { display: false } } } }
    });

    var cc = document.getElementById('chartContent');
    if (cc) new Chart(cc, {
      type: 'doughnut',
      data: { labels: content.map(function(c) { return c.type; }), datasets: [{ data: content.map(function(c) { return c.count; }), backgroundColor: content.map(function(c) { return c.color; }), borderWidth: 2, borderColor: '#fff' }] },
      options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { position: 'right', labels: { boxWidth: 12, padding: 12 } } }, cutout: '58%' }
    });

    var cd = document.getElementById('chartDaily');
    if (cd) new Chart(cd, {
      type: 'line',
      data: {
        labels: daily.map(function(d) { return d.d; }),
        datasets: [
          { label: 'Impressions', data: daily.map(function(d) { return d.i; }), borderColor: '#93c5fd', backgroundColor: 'rgba(147,197,253,0.22)', fill: true, tension: 0.35, yAxisID: 'y' },
          { label: 'Clicks', data: daily.map(function(d) { return d.c; }), borderColor: '#2d7a5e', borderWidth: 2.5, pointRadius: 4, tension: 0.35, yAxisID: 'y1' }
        ]
      },
      options: { responsive: true, maintainAspectRatio: false, plugins: { legend: { position: 'bottom' } }, scales: { y: { position: 'left', grid: { color: '#f4f1eb' } }, y1: { position: 'right', grid: { display: false }, min: 0 } } }
    });
  }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', run);
  else run();
})();
</script>

</body>
</html>
