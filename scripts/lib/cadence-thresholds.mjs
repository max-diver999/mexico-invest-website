/**
 * Пороги AI-каденции для mexico-invest (английский корпус).
 *
 * Движок `scripts/lib/ai-cadence.mjs` и CLI `scripts/cadence-check.mjs` общие с
 * moregroupestate-ru и more-group-website, байт в байт. Разница между сайтами
 * живёт только здесь: язык шаблонов и потолки по коллекциям. Расхождение копий
 * движка ловит `npm run cadence:parity`.
 *
 * КАК МЕНЯТЬ ПОРОГИ. Числа посчитаны командой `node scripts/cadence-check.mjs
 * --calibrate` по живому корпусу и выведены одним правилом, без ручной подгонки:
 *   maxPer1k        = p90 плотности, округлённый до ближайших 0.5, но не ниже 2.5;
 *   maxMetronomePct = p90 метронома, округлённый вверх до кратного 5, в пределах 20..60;
 *   minCv           = p10 CV минус 0.05, округлённый вниз до 0.05, в пределах 0.25..0.35.
 * Профиль раздела заводится только там, где в разделе 8 и больше файлов длиннее
 * порога слов. Порог из головы здесь запрещён: гейт по GEO-баллу, выставленный
 * до замера, кончился тем, что машинные тексты набирали 92, а рукописные 78.
 *
 * Все разделы корпуса на английском (проверено по тексту 22.09.2026), поэтому
 * исключённых языковых разделов нет.
 */

export const LANG = 'en';

/** Замер 22.09.2026, 357 файлов длиннее 250 слов, ветка cc/cadence-port. */
export const CALIBRATION = {
  measuredAt: '2026-09-22',
  files: 357,
  corpus: {
    per1k: { p50: 2.40, p75: 3.90, p90: 5.68, max: 11.19 },
    metronomePct: { p50: 20, p75: 29, p90: 36, max: 50 },
    cv: { p10: 0.49, p25: 0.53, p50: 0.61 },
  },
  /**
   * Сумма по категориям на весь корпус: antithesis 1645 (4.61 на файл),
   * colonHook 110, superlativeOpener 34, calque 7, clerical 3, headingHook 2,
   * literary 0, impersonal 0.
   *
   * Картина та же, что на Пхукете: почти всю плотность даёт ОДНА фигура речи,
   * антитеза («a plan, not panic»). Канцелярит и пафос, которые ловил прежний
   * гейт (lib/more-content-gate.mjs), дают на весь корпус единицы попаданий.
   * Хуже всего новости: медиана 5.02 на 1000 слов, вдвое выше корпуса.
   */
  totals: {
    antithesis: 1645,
    colonHook: 110,
    superlativeOpener: 34,
    calque: 7,
    clerical: 3,
    headingHook: 2,
    literary: 0,
    impersonal: 0,
  },
  dominantCategory: 'antithesis',
};

export const CADENCE_THRESHOLDS = {
  // p75 3.90 / p90 5.68 -> 5.5
  maxPer1k: 5.5,
  // метроном p75 29 / p90 35.8 -> 40
  maxMetronomePct: 40,
  // CV p10 0.49 - 0.05 = 0.44 -> 0.40, потолок правила 0.35
  minCv: 0.35,
  maxParallel: 3,
  // Повтор n-граммы шумит на шаблонных карточках проектов: только отчёт.
  maxRepeated: null,
  // Ниже 300 слов плотность на 1000 слов перестаёт быть устойчивой: четыре
  // попадания на 264 слова дают 15/1k и выглядят как провал на ровном месте.
  minWords: 300,
};

/**
 * Профили по коллекциям, каждый по своим квантилям. У всех шести разделов
 * n >= 8 файлов длиннее порога слов, поэтому профиль есть у каждого.
 */
export const COLLECTION_PROFILES = {
  // n=43; плотность p75 2.60 / p90 2.93, метроном p90 31.5, CV p10 0.50
  areas: { maxPer1k: 3.0, maxMetronomePct: 35, minCv: 0.35 },
  // n=36; плотность p75 2.27 / p90 3.76, метроном p90 35.0, CV p10 0.50
  compare: { maxPer1k: 4.0, maxMetronomePct: 40, minCv: 0.35 },
  // n=9, карточки застройщиков; плотность p75 2.15 / p90 3.08, метроном p90 25.4,
  // CV p10 0.35 - 0.05 = 0.30 -> вниз 0.25: тексты ровные по шаблону карточки
  developers: { maxPer1k: 3.0, maxMetronomePct: 30, minCv: 0.25, minWords: 400, maxParallel: 4 },
  // n=141; плотность p75 4.59 / p90 5.63, метроном p90 34.6, CV p10 0.51.
  // По правилу вышло 5.5, но так помечалось 93 файла из 357 (26.1%, выше потолка
  // 25%), и больше всего пометок было здесь (46). Ослаблено на один шаг 0.5 до
  // 6.0: стало 89 из 357 (24.9%).
  guides: { maxPer1k: 6.0, maxMetronomePct: 35, minCv: 0.35 },
  // n=28; плотность p75 6.47 / p90 8.98, метроном p90 40.0, CV p10 0.51.
  // Самый густой раздел корпуса: порог высокий, потому что взят из данных, а не
  // потому что антитеза в новостях допустима.
  news: { maxPer1k: 9.0, maxMetronomePct: 40, minCv: 0.35, minWords: 350 },
  // n=100, карточки проектов; плотность p75 3.50 / p90 5.65, метроном p90 36.4, CV p10 0.48
  projects: { maxPer1k: 5.5, maxMetronomePct: 40, minCv: 0.35, minWords: 400, maxParallel: 4 },
};

export function cadenceProfileFor(collection) {
  return { ...CADENCE_THRESHOLDS, ...(COLLECTION_PROFILES[collection] || {}) };
}
